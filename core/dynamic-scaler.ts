#!/usr/bin/env bun
/**
 * Dynamic Agent Scaler
 * 
[truncated]
 this.executors.set(executor.id, executor);
    
    // Auto-scale down after work done
    setTi
[truncated]
aleInterval(() => {
  scaler.autoScale();
}, 30_000); // Check every 30s

export { scaler };
